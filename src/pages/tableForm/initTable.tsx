import { Table } from 'antd';
import React from 'react';
import { connect } from 'umi';

const TableForm = () => {
    return <div>123123</div>;
};

export default connect(({ login }: { login: any }) => ({ login }))(TableForm);
