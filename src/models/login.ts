import { Effect, Reducer } from 'umi';
import { work, remove } from '@/services/api';
import { message } from 'antd';

export default {
  namespace: 'login',

  state: {},

  effects: {},

  reducers: {},
};
